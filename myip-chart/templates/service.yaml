apiVersion: v1
kind: Service
metadata:
  name: {{ include "myip-chart.fullname" . }}
  labels:
    {{- include "myip-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myip-chart.selectorLabels" . | nindent 4 }}
