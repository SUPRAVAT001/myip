apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myip-chart.fullname" . }}-test-connection"
  labels:
    {{- include "myip-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myip-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
